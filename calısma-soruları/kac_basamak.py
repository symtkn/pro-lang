import doctest

def kac_basamak(sayi):
    """
    >>> kac_basamak(67853)
    5
    >>> kac_basamak(-8)
    1
    >>> kac_basamak(0)
    1
    """
    while sayi :
        a=len(str(sayi))
        return a
doctest.testmod()
